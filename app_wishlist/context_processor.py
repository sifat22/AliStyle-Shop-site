# from .models import Wishlist,WishlistItem
# from .views import _wishlist_id
# from app_store.models import Product
# from django.shortcuts import get_object_or_404


# def in_wishlist_item(request):
#     in_wishlist = WishlistItem.objects.filter(wishlist__id = _wishlist_id, product = single_product)

#     return dict(in_wishlist = in_wishlist)
    